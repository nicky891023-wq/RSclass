# AI 在空間/地理資料處理上的盲點與反思

## 觀察到的 AI 盲點與易錯點

### 1. 座標系統識別不足

#### 盲點描述
AI 在本次任務中未能主動識別台灣特有的座標系統問題。

#### 具體表現
- **未檢查 TWD97/TM2**: 台灣常用的 TWD97 經緯度座標和 TM2 投影座標未被識別
- **經緯度範圍假設**: 僅使用通用經緯度範圍 (經度 100-130, 緯度 15-30)，未考慮台灣特定範圍
- **座標系混淆**: 未檢查是否有 TWD97 TM2 座標被誤當作 WGS84 經緯度

#### 風險評估
- **高風險**: TWD97 TM2 座標值 (如 x: 200000-300000, y: 2400000-2800000) 若被當作經緯度，會被錯誤標記為異常
- **中風險**: 不同座標系的精度和用途差異可能影響應用

#### 改進建議
```python
# 應加入的座標系檢查
def check_coordinate_system(lon, lat):
    # WGS84 經緯度範圍 (台灣)
    if 118 <= lon <= 123 and 21 <= lat <= 26:
        return 'WGS84'
    # TWD97 TM2 範圍 (近似)
    elif 200000 <= lon <= 300000 and 2400000 <= lat <= 2800000:
        return 'TWD97_TM2'
    else:
        return 'UNKNOWN'
```

### 2. 經緯度互換檢測缺失

#### 盲點描述
AI 未檢查常見的經緯度互換錯誤。

#### 具體表現
- **未驗證合理性**: 對於 (緯度, 經度) 互換的座標未進行檢測
- **缺乏地理常識**: 未利用「台灣經度 > 緯度」的基本地理常識進行驗證

#### 實際案例
雖然本次資料未發現明顯的經緯度互換問題，但 AI 應具備此檢測能力。

#### 改進建議
```python
def detect_lat_lon_swap(df):
    swapped_suspicious = []
    for idx, row in df.iterrows():
        lon, lat = row['經度'], row['緯度']
        # 如果經度小於緯度，可能互換
        if pd.notna(lon) and pd.notna(lat) and lon < lat:
            swapped_suspicious.append(idx)
    return swapped_suspicious
```

### 3. 地址地理編碼能力不足

#### 盲點描述
AI 缺乏地址地理編碼驗證能力。

#### 具體表現
- **無法驗證地址**: 對於缺失座標的記錄，AI 無法透過地址推算座標
- **地址格式不識別**: 未檢查台灣地址格式的一致性
- **無法交叉驗證**: 座標與地址的對應關係未進行合理性檢查

#### 風險評估
- **中風險**: 無法自動修復缺失座標
- **低風險**: 無法識別地址錯誤

#### 改進建議
應整合地理編碼 API (如 Google Geocoding API 或台灣政府開放 API) 進行地址驗證。

### 4. 缺值處理過於保守

#### 盲點描述
AI 在處理缺值時過於保守，錯失了合理的推論機會。

#### 具體表現
- **零值當缺值**: 將容量為 0 的記錄直接視為問題，未考慮可能為正確值
- **不推論村里**: 對於缺失村里但地址完整的記錄，未嘗試從地址提取村里資訊
- **災害類別不推論**: 對於缺失災害類別的記錄，未根據地理位置或場所類型推論可能適用的災害類型

#### 改進建議
```python
def extract_village_from_address(address, county_town):
    # 從地址中提取村里資訊的邏輯
    if pd.notna(address):
        # 使用正則表達式或關鍵字匹配
        pass
```

### 5. 空間關係分析能力有限

#### 盲點描述
AI 缺乏空間關係分析能力。

#### 具體表現
- **重複座標分析不足**: 對於重複座標，未分析是否為合理情況 (如同一建物內多個設施)
- **空間分布檢查**: 未檢查點位的空間分布合理性
- **鄰近性分析**: 未檢查相近座標的記錄是否相關

#### 改進建議
應加入空間分析庫 (如 shapely, geopandas) 進行更深入的空间分析。

### 6. 本地化知識不足

#### 盲點描述
AI 對台灣地理特徵的本地化知識不足。

#### 具體表現
- **行政區劃不熟悉**: 對台灣縣市鄉鎮的層級關係不夠了解
- **地理常識缺乏**: 對台灣地理範圍、地形特徵的基本認識不足
- **地址格式不熟悉**: 對台灣地址格式 (如「段」、「鄰」、「號」) 的理解有限

#### 改進建議
應建立台灣地理相關的知識庫或規則集。

## 技術限制與挑戰

### 1. 工具限制
- **缺乏 GIS 工具**: 未使用專業的地理資訊系統工具
- **API 限制**: 無法即時調用地圖服務 API 進行驗證
- **計算資源**: 大規模空間計算可能超出現有資源限制

### 2. 資料品質限制
- **原始資料不完整**: 缺失值過多影響分析準確性
- **格式不一致**: 不同來源的資料格式差異大
- **元數據缺失**: 缺乏座標系統、測量精度等元數據

### 3. 演算法限制
- **機器學習偏差**: 訓練資料可能缺乏台灣本地案例
- **規則式限制**: 硬編碼規則無法處理所有例外情況
- **統計方法限制**: 傳統統計方法對空間資料的特殊性考慮不足

## 改進策略建議

### 1. 短期改進 (1-2 週)
- **建立座標系檢測**: 加入 TWD97/TM2 與 WGS84 的識別邏輯
- **經緯度互換檢測**: 實作基本的經緯度合理性檢查
- **地址格式驗證**: 建立台灣地址格式的基本規則

### 2. 中期改進 (1-2 月)
- **整合地理編碼 API**: 加入地址地理編碼驗證功能
- **空間分析工具**: 整合 geopandas 進行空間關係分析
- **本地化知識庫**: 建立台灣地理相關的規則和知識庫

### 3. 長期改進 (3-6 月)
- **機器學習模型**: 訓練專門處理台灣地理資料的模型
- **實時驗證系統**: 建立與地圖服務的實時驗證機制
- **專家系統**: 整合地理資訊專家知識

## 結論與建議

### 主要發現
1. **空間專業性不足**: AI 在處理地理資料時缺乏專業的 GIS 知識
2. **本地化能力有限**: 對台灣特有的地理環境和資料格式理解不足
3. **工具整合不夠**: 未充分利用專業的地理資訊處理工具
4. **推論能力保守**: 在合理推論方面過於保守，錯失修復機會

### 關鍵建議
1. **加強專業培訓**: AI 需要更多 GIS 和地理資訊系統的專業知識
2. **本地化學習**: 針對特定地區 (如台灣) 建立本地化的處理能力
3. **工具整合**: 整合更多專業的地理資訊處理工具和 API
4. **人機協作**: 建立專家驗證機制，彌補 AI 的不足

### 未來展望
隨著 AI 技術的發展和地理資訊處理工具的進步，預期 AI 在空間資料處理方面的能力將顯著提升。但專業的地理知識和本地化經驗仍將是人類專家的重要價值。

本次任務的經驗顯示，AI 在處理空間資料時需要更加謹慎，特別是在座標系統識別、地理編碼驗證等方面，建議採取更保守的處理策略，並建立完善的人機協作機制。
